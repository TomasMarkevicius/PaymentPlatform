# PaymentPlatform
A backend payment processing system, built for learning distributed systems and payment architecture.

## Setup

Install dependencies:

```bash
poetry install
```
Run the app:

```bash
poetry run uvicorn app.main:app --reload
```

## Requirements

- Python 3.10+
- Poetry (https://python-poetry.org/docs/#installation)